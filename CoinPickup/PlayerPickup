using UnityEngine;
namespace CollectingCoins

// attached to player

{
    public class PlayerPickup : MonoBehavior{
        [SerializeField] int coins;
        [SerializeField] TMPro.TextMeshPROUGUI coinText; // TMP attached to the counter
        
        public void Increment(){
            coins++;
            coinText.text = coins.ToString();
        }
    }
}
