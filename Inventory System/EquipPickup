using UnityEngine;
using StarterAssets;

public class EquipPickup : MonoBehavior{
    [SerializeField] EqEnum key;
    private void on TriggerEnter(Collider other){
        var player = other.GetComponent<ThirdPersonController();
        if(player){
            player.Add(key);
        }
    }
}