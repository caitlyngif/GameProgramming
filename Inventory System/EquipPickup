using UnityEngine;
using StarterAssets;

public class EquipPickup : MonoBehaviour{
    [SerializeField] EqEnum key;
    private void onTriggerEnter(Collider other){
        var player = other.GetComponent<Inventory>();
        if(player){
            player.Add(key);
        }
    }
}