using UnityEngine;
using StarterAssets;

public class EquipPickup : MonoBehavior{
    [SerializeField] EqEnum key;
    private void onTriggerEnter(Collider other){
        var player = other.GetComponent<Inventory>();
        if(player){
            player.Add(key);
        }
    }
}