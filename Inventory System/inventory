// added to player
using UnityEngine;

public class Inventory : MonoBehavior{
    List<EqEnum> items = new();
    [SerializeField] List<InventorySlot> slots = new();
    int available;

    internal void Enable(EqEnum key)
    {
        var item = map.First(z => z.key.Equals(key)).item;
        items.SetActive(true);
    }
    internal void Add(EqEnum key) 
    {
        if (available < slots.Count)
        {
            items.Add(key);
            slots[available++].Set(key);
        }
        else
        {
            Debug.Log("Inventory is full");
        }
    }
}